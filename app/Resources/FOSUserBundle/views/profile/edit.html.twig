{% extends 'layout.html.twig' %}
{% use 'templates/menu.html.twig' %}

{% block body %}
	<div class="container">
		<h1>Editer son profil</h1>
	</div>
	<hr/>

	<div class="row">
		<div class="col-sm-3"></div>
		<div class="col-sm-6">
			{{ form_start(form) }}
    		{{ form_widget(form) }}
			{{ form_end(form) }}
		</div>
		<div class="col-sm-3"></div>
	</div>

{% endblock %}