{% extends 'layout.html.twig' %}
{% use 'templates/menu.html.twig' %}

{% block body %}
	<div>
		
		<h1>{{ user.username }}</h1>

		<hr/>

		<h4>{{ user.firstName ~ " " ~ user.lastName }}</h4>

	</div>

{% endblock %}