{% extends 'layout.html.twig' %}

{% block body %}
	<div class="container">
		<h1>Inscription</h1>
	</div>

	<div class="row">
		<div class="col-sm-4"></div>
		<div class="col-sm-4">

			{{ form_start(form, {'method': 'post', 'action': path('fos_user_registration_register'), 'attr': {'novalidate': 'true','class': 'fos_user_registration_register'}}) }}
    		{{ form_widget(form) }}
			{{ form_end(form) }}

		</div>
		<div class="col-sm-4"></div>
	</div>
{% endblock %}